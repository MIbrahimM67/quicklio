import test from'node:test';import assert from'node:assert/strict';import{canvasForRatio,containRect,coverRect}from'../assets/js/social-fit-core.mjs';test('portrait canvas',()=>assert.deepEqual(canvasForRatio('portrait',1080),{width:1080,height:1350}));test('contain keeps whole image',()=>{const r=containRect(2000,1000,1080,1350);assert.equal(Math.round(r.width),1080);assert.ok(r.height<1350)});test('cover fills canvas',()=>{const r=coverRect(2000,1000,1080,1350);assert.ok(r.width>=1080&&r.height>=1350)});