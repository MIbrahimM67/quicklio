import test from'node:test';import assert from'node:assert/strict';import{calculateYarnFromSwatch}from'../assets/js/yarn-core.mjs';test('swatch scales by area',()=>{const r=calculateYarnFromSwatch({projectWidth:40,projectHeight:50,swatchWidth:4,swatchHeight:5,swatchYarn:10,bufferPercent:10,ballLength:220});assert.equal(r.baseYarn,1000);assert.equal(r.totalYarn,1100);assert.equal(r.balls,5)})