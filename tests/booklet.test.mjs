import test from'node:test';import assert from'node:assert/strict';import{signaturePlans,paperPoints}from'../assets/js/booklet-core.mjs';test('8 page booklet order',()=>{const p=signaturePlans(8,8)[0];assert.deepEqual(p.sheets[0],{front:[8,1],back:[2,7]});assert.deepEqual(p.sheets[1],{front:[6,3],back:[4,5]})});test('pads with blanks',()=>{const p=signaturePlans(6,8)[0];assert.equal(p.paddedPages,8);assert.equal(p.sheets[0].front[0],null)});test('letter landscape size',()=>assert.deepEqual(paperPoints('letter'),{width:792,height:612,label:'Letter landscape'}));