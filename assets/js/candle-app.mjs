import{calculateCandle,fmt}from"/assets/js/candle-core.mjs";const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];let mode="targetBlend";$$('[data-mode]').forEach(b=>b.onclick=()=>{mode=b.dataset.mode;$$('[data-mode]').forEach(x=>x.classList.toggle('active',x===b));$('#amountLabel').textContent=mode==='knownWax'?'Wax weight per candle':'Finished weight per candle';render()});['amount','qty','pct','basis','unit'].forEach(id=>$('#'+id).addEventListener('input',render));function render(){try{const r=calculateCandle({mode,basis:$('#basis').value,amount:$('#amount').value,fragrancePercent:$('#pct').value,quantity:Number($('#qty').value)}),u=$('#unit').value;$('#error').textContent='';$('#results').hidden=false;$('#wax').textContent=fmt(r.waxTotal,u);$('#frag').textContent=fmt(r.fragranceTotal,u);$('#blend').textContent=fmt(r.finishedTotal,u);$('#per').textContent=`Per candle: ${fmt(r.waxPer,u)} wax + ${fmt(r.fragrancePer,u)} fragrance`;$('#copy').onclick=async()=>navigator.clipboard.writeText(`Quicklio candle calculation
Wax: ${fmt(r.waxTotal,u)}
Fragrance: ${fmt(r.fragranceTotal,u)}
Finished: ${fmt(r.finishedTotal,u)}`)}catch(e){$('#results').hidden=true;$('#error').textContent=e.message}}$('#print').onclick=()=>print();render();
