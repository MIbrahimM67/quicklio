export const MM_TO_PT=72/25.4;export function marginToPoints(v,u){v=Number(v);if(!Number.isFinite(v)||v<=0)throw Error("Enter a margin greater than zero.");return u==='in'?v*72:v*MM_TO_PT}export function pageSide(n,a,s){return Math.abs(n-a)%2===0?s:(s==='right'?'left':'right')}export function bindingEdge(n,o){if(o.bindingSide!=='mirrored')return o.bindingSide;return pageSide(n,o.anchorPage,o.anchorSide)==='right'?'left':'right'}export function testIndexes(count,anchor){if(count<=1)return[0];const i=Math.max(0,Math.min(count-1,anchor-1));return i<count-1?[i,i+1]:[i-1,i]}